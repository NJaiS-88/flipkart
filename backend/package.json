{
  "name": "parking-hotspot-backend",
  "version": "1.0.0",
  "description": "Backend API for Parking Hotspot Inspector — Patrol Routes, Monthly Reports, Enforcement Analytics",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js",
    "build": "npm install && pip3 install -r ../requirements.txt --quiet || pip install -r ../requirements.txt --quiet || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "node-cron": "^4.4.1"
  }
}
