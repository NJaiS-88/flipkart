# ============================================================
# requirements.txt — Python dependencies for one.py & two.py
# Parking Violations Hotspot Inspector
# ============================================================

# Core data processing (one.py + two.py)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0

# Machine learning — officer clustering via KMeans (two.py)
scikit-learn>=1.3.0,<2.0.0

# PDF report generation (one.py)
reportlab>=4.0.0,<5.0.0

# HTTP Requests (MapmyIndia API in two.py)
requests>=2.31.0


# ============================================================
# Install: pip install -r requirements.txt
# Deployment (Render): pip3 install -r requirements.txt
# ============================================================
